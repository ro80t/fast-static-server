{
  "name": "fast-static-server-npm-workspace",
  "private": true,
  "workspaces": [
    "bench"
  ],
  "type": "module",
  "scripts": {
    "lint": "oxlint -c oxlint.config.ts .",
    "format": "oxfmt -c oxfmt.config.ts .",
    "format:check": "oxfmt -c oxfmt.config.ts --check ."
  },
  "devDependencies": {
    "oxfmt": "^0.68.0",
    "oxlint": "^1.83.0"
  }
}
