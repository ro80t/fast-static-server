{
  "name": "fast-static-server-workspace",
  "private": true,
  "workspaces": [
    "bench"
  ],
  "type": "module",
  "scripts": {
    "bench": "bun run --filter fast-static-server-bench bench",
    "lint": "oxlint -c oxlint.config.ts .",
    "format": "oxfmt -c oxfmt.config.ts .",
    "format:check": "oxfmt -c oxfmt.config.ts --check ."
  },
  "devDependencies": {
    "oxfmt": "^0.68.0",
    "oxlint": "^1.83.0"
  }
}
